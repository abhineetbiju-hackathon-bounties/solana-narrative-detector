# GitHub Personal Access Token (optional, increases rate limit from 60 to 5000 req/hour)
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Helius API Key (optional, enables richer onchain data + better RPC)
# Get one at: https://helius.dev (free tier available)
HELIUS_API_KEY=your_helius_key_here

# Solana RPC Endpoint (optional, defaults to public RPC which rate-limits at ~10 req/s)
# Helius provides a free RPC: https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
SOLANA_RPC=https://api.mainnet-beta.solana.com

# Twitter/X API Bearer Token (optional, enables direct tweet search via v2 API)
# Get one at: https://developer.twitter.com/en/portal/dashboard
# Requires at least Basic tier ($100/mo) for search/recent endpoint
# Without this, Twitter collection falls back to RSS/scraping (limited results)
TWITTER_BEARER_TOKEN=your_bearer_token_here

# Cron job authorization (required for Vercel cron endpoint)
CRON_SECRET=your_cron_secret_here
